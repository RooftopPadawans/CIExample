image: openjdk:11-jdk

stages:
  - test
  - build
cache:
  paths:
    - .gradle/wrapper
    - .gradle/caches

before_script:
    - chmod +x ./gradlew
    - export GRADLE_USER_HOME=$PWD/.gradle

lintDebug:
  stage: test
  script:
    - ./gradlew ktlintCheck
    - ./gradlew detekt

